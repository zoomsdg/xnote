# 2026-04-21 数据加密改造

## 目标

让笔记数据本身（而不仅仅是配置项）即便在设备被 root 的场景下也保持加密：
攻击者即使能直接读取 `/data/data/com.example.xnote/` 整个目录，落盘的笔记 DB
与图片 / 音频文件也都是密文，且密钥由 Android Keystore 持有、不可导出。

## 威胁模型变化

| 场景 | 改造前 | 改造后 |
|------|--------|--------|
| 普通其它 App 读笔记 | 系统沙箱阻止 ✅ | 系统沙箱 + 应用层加密 ✅✅ |
| Root 后 `adb pull` DB | 直接打开 SQLite 明文 ❌ | SQLCipher 4 加密，密钥在 Keystore ✅ |
| Root 后 `adb pull` 图片/音频 | 直接打开 JPEG/m4a ❌ | AES-256-GCM 加密，密钥在 Keystore ✅ |
| Root 后 dump SharedPreferences | 看到明文配置（除 EncryptedSharedPreferences） | wrapped passphrase 也只是密文 ✅ |
| 备份通道（云备份 / ADB backup） | `allowBackup=false` 已阻止 ✅ | 同 ✅ |
| 导出 ZIP 在 `/sdcard/Download/` | zip4j AES-256，依赖用户口令 | 同 |
| 运行期内存被 root 抓 | 明文可见 ❌ | 仍可见 ❌（无法消除，见下） |
| 播放/读音频时长瞬间 | 已是明文文件 | 短暂落 cache 明文，使用完立删 ⚠️ |

## 加密方案

- **主密钥**：`AndroidKeyStore` 中的 AES-256-GCM 密钥，alias `XNote_Data_MasterKey_v1`，
  优先尝试 `setIsStrongBoxBacked(true)`，失败回落 TEE。`setRandomizedEncryptionRequired(true)`，
  密钥不可导出。
- **数据库**：SQLCipher 4 默认参数（PBKDF2-HMAC-SHA512 256 000 轮、AES-256-CBC + HMAC-SHA512）。
  passphrase 为 32 字节随机数的 64 字符 hex 串，**用主密钥包封后存进 SharedPreferences**。
- **媒体文件**（图片 / 音频）：AES-256-GCM，每文件随机 12 字节 IV，认证标签 128 bit。
  落盘格式：

  ```
  [magic 4B "XNC1"][iv 12B][ciphertext + GCM tag]
  ```

- **解密临时文件**：仅当系统组件（MediaPlayer / MediaMetadataRetriever / 大图查看器）
  必须读真实文件时使用，落到 `cacheDir/decrypt_tmp/`，用完立删；进程下次启动也会清空残留。

## 一次性数据迁移

`XNoteApplication.onCreate()` 中调用 `LegacyDataMigrator.migrateIfNeeded(context)`：

1. **DB 迁移（同步）**：检测 `note_database` 是否以 `SQLite format 3` 开头；若是则用
   SQLCipher 的 `ATTACH ... KEY '...'` + `SELECT sqlcipher_export()` 把内容搬到加密 DB，
   保留 `user_version`，替换原文件并清理 `-journal / -wal / -shm`。
2. **媒体迁移（后台线程）**：扫描 `filesDir/{images,audios,audio}/`，对未带 `XNC1` 头的
   文件做就地加密。媒体读取链路全部使用 `MediaCryptor.readAll()`，自动兼容尚未迁移完的明文。

迁移幂等，任何时刻进程被杀重启都能继续推进。

## 新增文件

| 文件 | 作用 |
|------|------|
| [security/MasterKeyManager.kt](app/src/main/java/com/example/xnote/security/MasterKeyManager.kt) | Keystore 主密钥的创建/获取（StrongBox→TEE 回落） |
| [security/MediaCryptor.kt](app/src/main/java/com/example/xnote/security/MediaCryptor.kt) | 媒体文件 AES-GCM 加解密 / Bitmap 解码 / cache 临时件 |
| [security/DatabaseKeyProvider.kt](app/src/main/java/com/example/xnote/security/DatabaseKeyProvider.kt) | SQLCipher passphrase 的随机生成与 wrapped 持久化 |
| [security/LegacyDataMigrator.kt](app/src/main/java/com/example/xnote/security/LegacyDataMigrator.kt) | 一次性、幂等的明文→密文迁移 |
| [XNoteApplication.kt](app/src/main/java/com/example/xnote/XNoteApplication.kt) | `loadLibs` + 主密钥预热 + 迁移 + 临时件清理 + 配置初始化 |

## 修改文件

| 文件 | 关键改动 |
|------|----------|
| [app/build.gradle](app/build.gradle) | 加入 `net.zetetic:android-database-sqlcipher:4.5.4` 与 `androidx.sqlite:sqlite:2.3.1` |
| [app/src/main/AndroidManifest.xml](app/src/main/AndroidManifest.xml) | `application` 节点注册 `android:name=".XNoteApplication"` |
| [data/NoteDatabase.kt](app/src/main/java/com/example/xnote/data/NoteDatabase.kt) | `Room.databaseBuilder().openHelperFactory(SupportFactory(passphrase, null, false))` |
| [utils/FileUtils.kt](app/src/main/java/com/example/xnote/utils/FileUtils.kt) | `saveImage / saveAudioToPrivateStorage` 全部加密落盘；`getAudioDuration(context, path)` 解密到 cache 后再读；`getImageSize` 走 MediaCryptor |
| [utils/ImageUtils.kt](app/src/main/java/com/example/xnote/utils/ImageUtils.kt) | `saveBitmapToFile / compressImageFile / getImageDimensions` 全部加密化 |
| [audio/AudioRecorder.kt](app/src/main/java/com/example/xnote/audio/AudioRecorder.kt) | 录到 `cacheDir/rec_tmp/` 明文，stop 时加密落 `filesDir/audios/` 并删除明文 |
| [audio/AudioPlayer.kt](app/src/main/java/com/example/xnote/audio/AudioPlayer.kt) | 构造接收 `Context`；prepare 时遇 XNC1 文件解密到 cache 喂 MediaPlayer，release 时清理 |
| [ui/RichEditText.kt](app/src/main/java/com/example/xnote/ui/RichEditText.kt) | 内联预览图改 `MediaCryptor.decodeBitmapSampled` |
| [NoteEditActivity.kt](app/src/main/java/com/example/xnote/NoteEditActivity.kt) | 大图查看 `MediaCryptor.decodeBitmapFull`；`AudioPlayer(this)`；`getAudioDuration(this, ...)` |
| [ShareTargetActivity.kt](app/src/main/java/com/example/xnote/ShareTargetActivity.kt) | 分享进来的图片直接 `MediaCryptor.encryptStream` 落盘 |
| [repository/NoteRepository.kt](app/src/main/java/com/example/xnote/repository/NoteRepository.kt) | `importNote` 复制媒体改加密落盘；audio 目录统一为 `audios/` |
| [utils/ExportImportUtils.kt](app/src/main/java/com/example/xnote/utils/ExportImportUtils.kt) | 导出时把 XNC1 媒体**解密**进 ZIP 临时目录，便于跨设备/重装后导入 |
| [proguard-rules.pro](proguard-rules.pro) | `-keep class net.sqlcipher.**` 等保留规则 |

## 行为对比

### Note 文本块

- 改造前：`/data/data/com.example.xnote/databases/note_database` 是普通 SQLite，
  `sqlite3 note_database "select text from blocks;"` 直接出原文。
- 改造后：同路径文件以 SQLCipher 4 格式加密，无 passphrase 打开会 `file is not a database`。

### 图片 / 音频

- 改造前：`/data/data/com.example.xnote/files/images/img_*.jpg` 是标准 JPEG，
  `file` 命令识别为 `JPEG image data`。
- 改造后：同路径文件前 4 字节为 `XNC1`，`file` 命令识别为 `data`；用 BitmapFactory 直接解码失败。

### 录音

- 改造前：`MediaRecorder` 直接写到 `filesDir/audios/record_*.m4a`，落盘即明文。
- 改造后：先写 `cacheDir/rec_tmp/rec_*.m4a` 明文 → stop 时一次性 AES-GCM 加密到
  `filesDir/audios/audio_<uuid>.m4a` 后删除明文 → 失败时也走清理路径。

### 分享接收

- 改造前：从其它 App 分享进来的图片以明文 JPEG 持久化。
- 改造后：`saveSharedImage` 通过 `MediaCryptor.encryptStream` 直接加密落盘，
  压缩步骤 `compressImageFile` 也是「读入解密 → 压缩 → 加密回写」。

## 升级期 UX 影响

- 旧版本用户首启会在 `Application.onCreate` 同步执行 DB 迁移；笔记 DB 通常很小
  （≤ MB 量级），迁移耗时几百毫秒~几秒。
- 媒体迁移在后台线程异步进行，期间媒体读取链路自动兼容明文，用户无感。
- 后续启动 `Application.onCreate` 走 fast path：迁移检测到 DB 已加密、目录均为 XNC1，
  瞬间返回。

## 仍存在的、绕不过去的限制

1. **运行期内存**：笔记被显示时仍以明文 String / Bitmap 存在 RAM。能在 app 运行时
   dump 进程内存的 root 攻击者仍能拿到原文。要彻底消除需要 `setUserAuthenticationRequired(true)`
   + 每次操作都生物识别，UX 代价极大，本次未默认开启。
2. **解密临时文件**：MediaPlayer / MediaMetadataRetriever / 大图查看器需要真实 File
   作为输入；存在「正在播放音频时被 root 抓盘 cache 目录」的短窗口。使用完立删 +
   进程启动清空残留是当前缓解。
3. **相机拍照路径**：`createTempCameraFile` 给系统相机的仍是 cacheDir 明文文件。
   现有 `takePictureLauncher` 的 `if (success)` 分支是空实现（拍照本未持久化进笔记），
   故未补加密；以后若补全，记得 `MediaCryptor.encryptInPlace(file)` 后再写入 DB。
4. **StrongBox 仅在 API 28+ 且硬件支持时生效**；老/便宜设备落回 TEE，仍优于无 Keystore。
5. **本次改动未在真机上做编译/运行验证**；本仓库不含 Android SDK 环境。如有 kapt /
   依赖版本相关的小报错，按报错调即可，核心方案不受影响。
6. **`build.gradle` 中 `signingConfigs.release` 仍硬编码 keystore 路径与密码**
   （`storePassword "zjdszs"`），与本次安全主题正交但强烈建议另行清理（移到本地
   `gradle.properties` / 环境变量 / Gradle Secrets）。

## 关键 API 速查

```kotlin
// 主密钥（一般无需直接调用）
MasterKeyManager.getOrCreateKey(): SecretKey

// 媒体文件加解密
MediaCryptor.encryptBytes(plain: ByteArray, dest: File)
MediaCryptor.encryptStream(input: InputStream, dest: File)
MediaCryptor.encryptInPlace(file: File)        // 明文 → XNC1，幂等
MediaCryptor.isEncryptedFile(file: File): Boolean
MediaCryptor.readAll(file: File): ByteArray    // 自动兼容明文
MediaCryptor.openInput(file: File): InputStream
MediaCryptor.decryptToCache(context, src, suffix): File
MediaCryptor.releaseDecryptedTemp(tmp: File?)
MediaCryptor.decodeBitmapBounds(file: File): Options
MediaCryptor.decodeBitmapSampled(file: File, maxW: Int, maxH: Int): Bitmap?
MediaCryptor.decodeBitmapFull(file: File): Bitmap?

// SQLCipher passphrase（一般也无需直接调用，Room 自动用）
DatabaseKeyProvider.getOrCreatePassphrase(context: Context): String
```
